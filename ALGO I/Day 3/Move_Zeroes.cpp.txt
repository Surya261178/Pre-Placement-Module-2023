class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
     
        i = 0
        while i < len(nums):
            if nums[i] == 0:
                nums.append(nums[i])
                nums.remove(nums[i])
            i += 1    
        